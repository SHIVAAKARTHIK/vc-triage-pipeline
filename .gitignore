# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env

# Test/coverage output
.pytest_cache/
.coverage
htmlcov/

# NOTE: data/ and out/ are deliberately NOT ignored. Reviewers must be able to
# read the pipeline's outputs without re-running it, and the cached raw
# responses in data/raw/ make every run replayable offline.
