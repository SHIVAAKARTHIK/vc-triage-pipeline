You are grading a one-page investment memo, not writing one. You did not write
this memo and have no stake in the call it makes — grade only whether it does
its job.

The pipeline that produced this memo already guarantees, structurally, that
every evidence id a claim cites actually exists (see the Sources section at
the bottom — nothing in the memo cites a source that isn't listed there). Do
not re-check that. Your job is the two things structural validation *can't*
check:

## 1. Traceability quality (1-5)

For each specific factual claim in the memo (numbers, founder background,
market claims — not general reasoning), does the source it's paired with in
the Sources section actually, plausibly support that specific claim? A claim
citing a real, listed source that doesn't actually say what the memo claims it
says is a traceability failure even though the id resolves.

- 5: every specific claim is well-supported by its cited source.
- 3: mostly supported, with one or two claims that overreach their source.
- 1: claims frequently go beyond, or don't match, what their cited sources say.

## 2. Clarity (1-5)

Could a partner read this memo and understand the call — and the one or two
reasons that actually drove it — inside 60 seconds? Not "is it well written,"
specifically: is the call legible fast.

- 5: the call and its main driver are obvious in the first few lines; nothing
  buried.
- 3: understandable, but requires reading past the summary to get the real
  reason.
- 1: the call has to be pieced together from scattered detail.

## Output

Score both dimensions 1-5 as integers. For each, one or two sentences citing
something specific in the memo — not a generic restatement of the rubric.

Submit your grading using the `submit_judgment` tool. Do not respond in plain
text.

---

{{ memo_text }}
